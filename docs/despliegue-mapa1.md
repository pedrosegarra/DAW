mindmap
  root((Despliegue de aplicaciones web))
    Control de versiones (Git/GitHub)
      VCS: concepto & utilidad
      Git básico
        init / clone / add / commit / status / log
        branch / merge / rebase
      Remotos
        GitHub: push/pull
        Pull requests & issues
    Virtualización y contenedores
      Nube (AWS Academy)
        EC2 / redes / seguridad básica
      Contenedores (Docker)
        Instalación
        Imágenes (build, pull)
        Contenedores (run, logs, exec)
        Compose (servicios multi-contenedor)
    Servidores web
      Protocolos
        HTTP / HTTPS (TLS)
      Apache
        Instalación
        Virtual hosts
        Módulos
      Nginx
        Instalación
        Server blocks
        Reverse proxy
    Servidores de aplicaciones
      Necesidad de contenido dinámico
      Tomcat (Java)
        Instalación
        Deploy WAR
        Conector con Apache/Nginx
    Servicios de red
      FTP
        Instalación
        Usuarios y permisos
      DNS (BIND)
        Resolución directa e inversa
        Maestro / esclavo / caché / forwarder
        Herramientas: dig, nslookup
      LDAP (OpenLDAP)
        Estructura y entradas
        Autenticación y autorización
        Integración con servicios
    CI/CD
      Integración continua
        Tests automáticos
      Despliegue continuo
        Pipelines
        Versionado y artefactos
      Ejemplos de herramientas
        GitHub Actions / Jenkins / GitLab CI
    Metodología del módulo
      Teoría + prácticas guiadas
      Entornos
        AWS (VMs)
        Docker (contenedores)
